## NOTE : Requirement for running the code in files

to Run the files create file as crredential.py 
should contain TOTP, USERID, API key, H_api, XLpath for json download, bot_token for telegram and channel_id
